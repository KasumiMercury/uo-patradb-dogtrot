// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/KasumiMercury/uo-patradb-dogtrot/ent/categorydescriptiontemplate"
)

// CategoryDescriptionTemplate is the model entity for the CategoryDescriptionTemplate schema.
type CategoryDescriptionTemplate struct {
	config `json:"-"`
	// ID of the ent.
	ID string `json:"id,omitempty"`
	// Text holds the value of the "text" field.
	Text string `json:"text,omitempty"`
	// StartUseAt holds the value of the "start_use_at" field.
	StartUseAt time.Time `json:"start_use_at,omitempty"`
	// LastUseAt holds the value of the "last_use_at" field.
	LastUseAt time.Time `json:"last_use_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the CategoryDescriptionTemplateQuery when eager-loading is set.
	Edges        CategoryDescriptionTemplateEdges `json:"edges"`
	selectValues sql.SelectValues
}

// CategoryDescriptionTemplateEdges holds the relations/edges for other nodes in the graph.
type CategoryDescriptionTemplateEdges struct {
	// Descriptions holds the value of the descriptions edge.
	Descriptions []*Description `json:"descriptions,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// DescriptionsOrErr returns the Descriptions value or an error if the edge
// was not loaded in eager-loading.
func (e CategoryDescriptionTemplateEdges) DescriptionsOrErr() ([]*Description, error) {
	if e.loadedTypes[0] {
		return e.Descriptions, nil
	}
	return nil, &NotLoadedError{edge: "descriptions"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*CategoryDescriptionTemplate) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case categorydescriptiontemplate.FieldID, categorydescriptiontemplate.FieldText:
			values[i] = new(sql.NullString)
		case categorydescriptiontemplate.FieldStartUseAt, categorydescriptiontemplate.FieldLastUseAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the CategoryDescriptionTemplate fields.
func (cdt *CategoryDescriptionTemplate) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case categorydescriptiontemplate.FieldID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				cdt.ID = value.String
			}
		case categorydescriptiontemplate.FieldText:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field text", values[i])
			} else if value.Valid {
				cdt.Text = value.String
			}
		case categorydescriptiontemplate.FieldStartUseAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field start_use_at", values[i])
			} else if value.Valid {
				cdt.StartUseAt = value.Time
			}
		case categorydescriptiontemplate.FieldLastUseAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field last_use_at", values[i])
			} else if value.Valid {
				cdt.LastUseAt = value.Time
			}
		default:
			cdt.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the CategoryDescriptionTemplate.
// This includes values selected through modifiers, order, etc.
func (cdt *CategoryDescriptionTemplate) Value(name string) (ent.Value, error) {
	return cdt.selectValues.Get(name)
}

// QueryDescriptions queries the "descriptions" edge of the CategoryDescriptionTemplate entity.
func (cdt *CategoryDescriptionTemplate) QueryDescriptions() *DescriptionQuery {
	return NewCategoryDescriptionTemplateClient(cdt.config).QueryDescriptions(cdt)
}

// Update returns a builder for updating this CategoryDescriptionTemplate.
// Note that you need to call CategoryDescriptionTemplate.Unwrap() before calling this method if this CategoryDescriptionTemplate
// was returned from a transaction, and the transaction was committed or rolled back.
func (cdt *CategoryDescriptionTemplate) Update() *CategoryDescriptionTemplateUpdateOne {
	return NewCategoryDescriptionTemplateClient(cdt.config).UpdateOne(cdt)
}

// Unwrap unwraps the CategoryDescriptionTemplate entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (cdt *CategoryDescriptionTemplate) Unwrap() *CategoryDescriptionTemplate {
	_tx, ok := cdt.config.driver.(*txDriver)
	if !ok {
		panic("ent: CategoryDescriptionTemplate is not a transactional entity")
	}
	cdt.config.driver = _tx.drv
	return cdt
}

// String implements the fmt.Stringer.
func (cdt *CategoryDescriptionTemplate) String() string {
	var builder strings.Builder
	builder.WriteString("CategoryDescriptionTemplate(")
	builder.WriteString(fmt.Sprintf("id=%v, ", cdt.ID))
	builder.WriteString("text=")
	builder.WriteString(cdt.Text)
	builder.WriteString(", ")
	builder.WriteString("start_use_at=")
	builder.WriteString(cdt.StartUseAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("last_use_at=")
	builder.WriteString(cdt.LastUseAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// CategoryDescriptionTemplates is a parsable slice of CategoryDescriptionTemplate.
type CategoryDescriptionTemplates []*CategoryDescriptionTemplate
