// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/KasumiMercury/uo-patradb-dogtrot/ent/description"
	"github.com/KasumiMercury/uo-patradb-dogtrot/ent/descriptionchange"
)

// DescriptionChange is the model entity for the DescriptionChange schema.
type DescriptionChange struct {
	config `json:"-"`
	// ID of the ent.
	ID string `json:"id,omitempty"`
	// Raw holds the value of the "raw" field.
	Raw string `json:"raw,omitempty"`
	// Variable holds the value of the "variable" field.
	Variable string `json:"variable,omitempty"`
	// ChangedAt holds the value of the "changed_at" field.
	ChangedAt time.Time `json:"changed_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the DescriptionChangeQuery when eager-loading is set.
	Edges          DescriptionChangeEdges `json:"edges"`
	description_id *string
	selectValues   sql.SelectValues
}

// DescriptionChangeEdges holds the relations/edges for other nodes in the graph.
type DescriptionChangeEdges struct {
	// Description holds the value of the description edge.
	Description *Description `json:"description,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// DescriptionOrErr returns the Description value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e DescriptionChangeEdges) DescriptionOrErr() (*Description, error) {
	if e.loadedTypes[0] {
		if e.Description == nil {
			// Edge was loaded but was not found.
			return nil, &NotFoundError{label: description.Label}
		}
		return e.Description, nil
	}
	return nil, &NotLoadedError{edge: "description"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*DescriptionChange) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case descriptionchange.FieldID, descriptionchange.FieldRaw, descriptionchange.FieldVariable:
			values[i] = new(sql.NullString)
		case descriptionchange.FieldChangedAt:
			values[i] = new(sql.NullTime)
		case descriptionchange.ForeignKeys[0]: // description_id
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the DescriptionChange fields.
func (dc *DescriptionChange) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case descriptionchange.FieldID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				dc.ID = value.String
			}
		case descriptionchange.FieldRaw:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field raw", values[i])
			} else if value.Valid {
				dc.Raw = value.String
			}
		case descriptionchange.FieldVariable:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field variable", values[i])
			} else if value.Valid {
				dc.Variable = value.String
			}
		case descriptionchange.FieldChangedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field changed_at", values[i])
			} else if value.Valid {
				dc.ChangedAt = value.Time
			}
		case descriptionchange.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field description_id", values[i])
			} else if value.Valid {
				dc.description_id = new(string)
				*dc.description_id = value.String
			}
		default:
			dc.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the DescriptionChange.
// This includes values selected through modifiers, order, etc.
func (dc *DescriptionChange) Value(name string) (ent.Value, error) {
	return dc.selectValues.Get(name)
}

// QueryDescription queries the "description" edge of the DescriptionChange entity.
func (dc *DescriptionChange) QueryDescription() *DescriptionQuery {
	return NewDescriptionChangeClient(dc.config).QueryDescription(dc)
}

// Update returns a builder for updating this DescriptionChange.
// Note that you need to call DescriptionChange.Unwrap() before calling this method if this DescriptionChange
// was returned from a transaction, and the transaction was committed or rolled back.
func (dc *DescriptionChange) Update() *DescriptionChangeUpdateOne {
	return NewDescriptionChangeClient(dc.config).UpdateOne(dc)
}

// Unwrap unwraps the DescriptionChange entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (dc *DescriptionChange) Unwrap() *DescriptionChange {
	_tx, ok := dc.config.driver.(*txDriver)
	if !ok {
		panic("ent: DescriptionChange is not a transactional entity")
	}
	dc.config.driver = _tx.drv
	return dc
}

// String implements the fmt.Stringer.
func (dc *DescriptionChange) String() string {
	var builder strings.Builder
	builder.WriteString("DescriptionChange(")
	builder.WriteString(fmt.Sprintf("id=%v, ", dc.ID))
	builder.WriteString("raw=")
	builder.WriteString(dc.Raw)
	builder.WriteString(", ")
	builder.WriteString("variable=")
	builder.WriteString(dc.Variable)
	builder.WriteString(", ")
	builder.WriteString("changed_at=")
	builder.WriteString(dc.ChangedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// DescriptionChanges is a parsable slice of DescriptionChange.
type DescriptionChanges []*DescriptionChange
