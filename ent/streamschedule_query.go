// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/KasumiMercury/uo-patradb-dogtrot/ent/predicate"
	"github.com/KasumiMercury/uo-patradb-dogtrot/ent/streamschedule"
	"github.com/KasumiMercury/uo-patradb-dogtrot/ent/video"
)

// StreamScheduleQuery is the builder for querying StreamSchedule entities.
type StreamScheduleQuery struct {
	config
	ctx        *QueryContext
	order      []streamschedule.OrderOption
	inters     []Interceptor
	predicates []predicate.StreamSchedule
	withVideos *VideoQuery
	withFKs    bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the StreamScheduleQuery builder.
func (ssq *StreamScheduleQuery) Where(ps ...predicate.StreamSchedule) *StreamScheduleQuery {
	ssq.predicates = append(ssq.predicates, ps...)
	return ssq
}

// Limit the number of records to be returned by this query.
func (ssq *StreamScheduleQuery) Limit(limit int) *StreamScheduleQuery {
	ssq.ctx.Limit = &limit
	return ssq
}

// Offset to start from.
func (ssq *StreamScheduleQuery) Offset(offset int) *StreamScheduleQuery {
	ssq.ctx.Offset = &offset
	return ssq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (ssq *StreamScheduleQuery) Unique(unique bool) *StreamScheduleQuery {
	ssq.ctx.Unique = &unique
	return ssq
}

// Order specifies how the records should be ordered.
func (ssq *StreamScheduleQuery) Order(o ...streamschedule.OrderOption) *StreamScheduleQuery {
	ssq.order = append(ssq.order, o...)
	return ssq
}

// QueryVideos chains the current query on the "videos" edge.
func (ssq *StreamScheduleQuery) QueryVideos() *VideoQuery {
	query := (&VideoClient{config: ssq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := ssq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := ssq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(streamschedule.Table, streamschedule.FieldID, selector),
			sqlgraph.To(video.Table, video.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, streamschedule.VideosTable, streamschedule.VideosColumn),
		)
		fromU = sqlgraph.SetNeighbors(ssq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first StreamSchedule entity from the query.
// Returns a *NotFoundError when no StreamSchedule was found.
func (ssq *StreamScheduleQuery) First(ctx context.Context) (*StreamSchedule, error) {
	nodes, err := ssq.Limit(1).All(setContextOp(ctx, ssq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{streamschedule.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (ssq *StreamScheduleQuery) FirstX(ctx context.Context) *StreamSchedule {
	node, err := ssq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first StreamSchedule ID from the query.
// Returns a *NotFoundError when no StreamSchedule ID was found.
func (ssq *StreamScheduleQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = ssq.Limit(1).IDs(setContextOp(ctx, ssq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{streamschedule.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (ssq *StreamScheduleQuery) FirstIDX(ctx context.Context) string {
	id, err := ssq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single StreamSchedule entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one StreamSchedule entity is found.
// Returns a *NotFoundError when no StreamSchedule entities are found.
func (ssq *StreamScheduleQuery) Only(ctx context.Context) (*StreamSchedule, error) {
	nodes, err := ssq.Limit(2).All(setContextOp(ctx, ssq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{streamschedule.Label}
	default:
		return nil, &NotSingularError{streamschedule.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (ssq *StreamScheduleQuery) OnlyX(ctx context.Context) *StreamSchedule {
	node, err := ssq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only StreamSchedule ID in the query.
// Returns a *NotSingularError when more than one StreamSchedule ID is found.
// Returns a *NotFoundError when no entities are found.
func (ssq *StreamScheduleQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = ssq.Limit(2).IDs(setContextOp(ctx, ssq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{streamschedule.Label}
	default:
		err = &NotSingularError{streamschedule.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (ssq *StreamScheduleQuery) OnlyIDX(ctx context.Context) string {
	id, err := ssq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of StreamSchedules.
func (ssq *StreamScheduleQuery) All(ctx context.Context) ([]*StreamSchedule, error) {
	ctx = setContextOp(ctx, ssq.ctx, "All")
	if err := ssq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*StreamSchedule, *StreamScheduleQuery]()
	return withInterceptors[[]*StreamSchedule](ctx, ssq, qr, ssq.inters)
}

// AllX is like All, but panics if an error occurs.
func (ssq *StreamScheduleQuery) AllX(ctx context.Context) []*StreamSchedule {
	nodes, err := ssq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of StreamSchedule IDs.
func (ssq *StreamScheduleQuery) IDs(ctx context.Context) (ids []string, err error) {
	if ssq.ctx.Unique == nil && ssq.path != nil {
		ssq.Unique(true)
	}
	ctx = setContextOp(ctx, ssq.ctx, "IDs")
	if err = ssq.Select(streamschedule.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (ssq *StreamScheduleQuery) IDsX(ctx context.Context) []string {
	ids, err := ssq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (ssq *StreamScheduleQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, ssq.ctx, "Count")
	if err := ssq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, ssq, querierCount[*StreamScheduleQuery](), ssq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (ssq *StreamScheduleQuery) CountX(ctx context.Context) int {
	count, err := ssq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (ssq *StreamScheduleQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, ssq.ctx, "Exist")
	switch _, err := ssq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (ssq *StreamScheduleQuery) ExistX(ctx context.Context) bool {
	exist, err := ssq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the StreamScheduleQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (ssq *StreamScheduleQuery) Clone() *StreamScheduleQuery {
	if ssq == nil {
		return nil
	}
	return &StreamScheduleQuery{
		config:     ssq.config,
		ctx:        ssq.ctx.Clone(),
		order:      append([]streamschedule.OrderOption{}, ssq.order...),
		inters:     append([]Interceptor{}, ssq.inters...),
		predicates: append([]predicate.StreamSchedule{}, ssq.predicates...),
		withVideos: ssq.withVideos.Clone(),
		// clone intermediate query.
		sql:  ssq.sql.Clone(),
		path: ssq.path,
	}
}

// WithVideos tells the query-builder to eager-load the nodes that are connected to
// the "videos" edge. The optional arguments are used to configure the query builder of the edge.
func (ssq *StreamScheduleQuery) WithVideos(opts ...func(*VideoQuery)) *StreamScheduleQuery {
	query := (&VideoClient{config: ssq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	ssq.withVideos = query
	return ssq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		ScheduledAt time.Time `json:"scheduled_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.StreamSchedule.Query().
//		GroupBy(streamschedule.FieldScheduledAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (ssq *StreamScheduleQuery) GroupBy(field string, fields ...string) *StreamScheduleGroupBy {
	ssq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &StreamScheduleGroupBy{build: ssq}
	grbuild.flds = &ssq.ctx.Fields
	grbuild.label = streamschedule.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		ScheduledAt time.Time `json:"scheduled_at,omitempty"`
//	}
//
//	client.StreamSchedule.Query().
//		Select(streamschedule.FieldScheduledAt).
//		Scan(ctx, &v)
func (ssq *StreamScheduleQuery) Select(fields ...string) *StreamScheduleSelect {
	ssq.ctx.Fields = append(ssq.ctx.Fields, fields...)
	sbuild := &StreamScheduleSelect{StreamScheduleQuery: ssq}
	sbuild.label = streamschedule.Label
	sbuild.flds, sbuild.scan = &ssq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a StreamScheduleSelect configured with the given aggregations.
func (ssq *StreamScheduleQuery) Aggregate(fns ...AggregateFunc) *StreamScheduleSelect {
	return ssq.Select().Aggregate(fns...)
}

func (ssq *StreamScheduleQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range ssq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, ssq); err != nil {
				return err
			}
		}
	}
	for _, f := range ssq.ctx.Fields {
		if !streamschedule.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if ssq.path != nil {
		prev, err := ssq.path(ctx)
		if err != nil {
			return err
		}
		ssq.sql = prev
	}
	return nil
}

func (ssq *StreamScheduleQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*StreamSchedule, error) {
	var (
		nodes       = []*StreamSchedule{}
		withFKs     = ssq.withFKs
		_spec       = ssq.querySpec()
		loadedTypes = [1]bool{
			ssq.withVideos != nil,
		}
	)
	if ssq.withVideos != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, streamschedule.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*StreamSchedule).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &StreamSchedule{config: ssq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, ssq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := ssq.withVideos; query != nil {
		if err := ssq.loadVideos(ctx, query, nodes, nil,
			func(n *StreamSchedule, e *Video) { n.Edges.Videos = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (ssq *StreamScheduleQuery) loadVideos(ctx context.Context, query *VideoQuery, nodes []*StreamSchedule, init func(*StreamSchedule), assign func(*StreamSchedule, *Video)) error {
	ids := make([]string, 0, len(nodes))
	nodeids := make(map[string][]*StreamSchedule)
	for i := range nodes {
		if nodes[i].video_id == nil {
			continue
		}
		fk := *nodes[i].video_id
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(video.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "video_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (ssq *StreamScheduleQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := ssq.querySpec()
	_spec.Node.Columns = ssq.ctx.Fields
	if len(ssq.ctx.Fields) > 0 {
		_spec.Unique = ssq.ctx.Unique != nil && *ssq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, ssq.driver, _spec)
}

func (ssq *StreamScheduleQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(streamschedule.Table, streamschedule.Columns, sqlgraph.NewFieldSpec(streamschedule.FieldID, field.TypeString))
	_spec.From = ssq.sql
	if unique := ssq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if ssq.path != nil {
		_spec.Unique = true
	}
	if fields := ssq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, streamschedule.FieldID)
		for i := range fields {
			if fields[i] != streamschedule.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := ssq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := ssq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := ssq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := ssq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (ssq *StreamScheduleQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(ssq.driver.Dialect())
	t1 := builder.Table(streamschedule.Table)
	columns := ssq.ctx.Fields
	if len(columns) == 0 {
		columns = streamschedule.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if ssq.sql != nil {
		selector = ssq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if ssq.ctx.Unique != nil && *ssq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range ssq.predicates {
		p(selector)
	}
	for _, p := range ssq.order {
		p(selector)
	}
	if offset := ssq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := ssq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// StreamScheduleGroupBy is the group-by builder for StreamSchedule entities.
type StreamScheduleGroupBy struct {
	selector
	build *StreamScheduleQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (ssgb *StreamScheduleGroupBy) Aggregate(fns ...AggregateFunc) *StreamScheduleGroupBy {
	ssgb.fns = append(ssgb.fns, fns...)
	return ssgb
}

// Scan applies the selector query and scans the result into the given value.
func (ssgb *StreamScheduleGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ssgb.build.ctx, "GroupBy")
	if err := ssgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*StreamScheduleQuery, *StreamScheduleGroupBy](ctx, ssgb.build, ssgb, ssgb.build.inters, v)
}

func (ssgb *StreamScheduleGroupBy) sqlScan(ctx context.Context, root *StreamScheduleQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(ssgb.fns))
	for _, fn := range ssgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*ssgb.flds)+len(ssgb.fns))
		for _, f := range *ssgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*ssgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ssgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// StreamScheduleSelect is the builder for selecting fields of StreamSchedule entities.
type StreamScheduleSelect struct {
	*StreamScheduleQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (sss *StreamScheduleSelect) Aggregate(fns ...AggregateFunc) *StreamScheduleSelect {
	sss.fns = append(sss.fns, fns...)
	return sss
}

// Scan applies the selector query and scans the result into the given value.
func (sss *StreamScheduleSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, sss.ctx, "Select")
	if err := sss.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*StreamScheduleQuery, *StreamScheduleSelect](ctx, sss.StreamScheduleQuery, sss, sss.inters, v)
}

func (sss *StreamScheduleSelect) sqlScan(ctx context.Context, root *StreamScheduleQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(sss.fns))
	for _, fn := range sss.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*sss.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := sss.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
