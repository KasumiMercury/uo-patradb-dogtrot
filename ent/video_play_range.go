// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/KasumiMercury/uo-patradb-dogtrot/ent/schema/pulid"
	"github.com/KasumiMercury/uo-patradb-dogtrot/ent/video_play_range"
)

// Video_play_range is the model entity for the Video_play_range schema.
type Video_play_range struct {
	config `json:"-"`
	// ID of the ent.
	ID pulid.ID `json:"id,omitempty"`
	// StartSeconds holds the value of the "start_seconds" field.
	StartSeconds int `json:"start_seconds,omitempty"`
	// EndSeconds holds the value of the "end_seconds" field.
	EndSeconds int `json:"end_seconds,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the Video_play_rangeQuery when eager-loading is set.
	Edges        Video_play_rangeEdges `json:"edges"`
	selectValues sql.SelectValues
}

// Video_play_rangeEdges holds the relations/edges for other nodes in the graph.
type Video_play_rangeEdges struct {
	// Video holds the value of the video edge.
	Video []*Video `json:"video,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// VideoOrErr returns the Video value or an error if the edge
// was not loaded in eager-loading.
func (e Video_play_rangeEdges) VideoOrErr() ([]*Video, error) {
	if e.loadedTypes[0] {
		return e.Video, nil
	}
	return nil, &NotLoadedError{edge: "video"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Video_play_range) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case video_play_range.FieldStartSeconds, video_play_range.FieldEndSeconds:
			values[i] = new(sql.NullInt64)
		case video_play_range.FieldID:
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Video_play_range fields.
func (vpr *Video_play_range) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case video_play_range.FieldID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				vpr.ID = pulid.ID(value.String)
			}
		case video_play_range.FieldStartSeconds:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field start_seconds", values[i])
			} else if value.Valid {
				vpr.StartSeconds = int(value.Int64)
			}
		case video_play_range.FieldEndSeconds:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field end_seconds", values[i])
			} else if value.Valid {
				vpr.EndSeconds = int(value.Int64)
			}
		default:
			vpr.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Video_play_range.
// This includes values selected through modifiers, order, etc.
func (vpr *Video_play_range) Value(name string) (ent.Value, error) {
	return vpr.selectValues.Get(name)
}

// QueryVideo queries the "video" edge of the Video_play_range entity.
func (vpr *Video_play_range) QueryVideo() *VideoQuery {
	return NewVideoPlayRangeClient(vpr.config).QueryVideo(vpr)
}

// Update returns a builder for updating this Video_play_range.
// Note that you need to call Video_play_range.Unwrap() before calling this method if this Video_play_range
// was returned from a transaction, and the transaction was committed or rolled back.
func (vpr *Video_play_range) Update() *VideoPlayRangeUpdateOne {
	return NewVideoPlayRangeClient(vpr.config).UpdateOne(vpr)
}

// Unwrap unwraps the Video_play_range entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (vpr *Video_play_range) Unwrap() *Video_play_range {
	_tx, ok := vpr.config.driver.(*txDriver)
	if !ok {
		panic("ent: Video_play_range is not a transactional entity")
	}
	vpr.config.driver = _tx.drv
	return vpr
}

// String implements the fmt.Stringer.
func (vpr *Video_play_range) String() string {
	var builder strings.Builder
	builder.WriteString("Video_play_range(")
	builder.WriteString(fmt.Sprintf("id=%v, ", vpr.ID))
	builder.WriteString("start_seconds=")
	builder.WriteString(fmt.Sprintf("%v", vpr.StartSeconds))
	builder.WriteString(", ")
	builder.WriteString("end_seconds=")
	builder.WriteString(fmt.Sprintf("%v", vpr.EndSeconds))
	builder.WriteByte(')')
	return builder.String()
}

// Video_play_ranges is a parsable slice of Video_play_range.
type Video_play_ranges []*Video_play_range
