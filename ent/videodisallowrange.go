// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/KasumiMercury/uo-patradb-dogtrot/ent/video"
	"github.com/KasumiMercury/uo-patradb-dogtrot/ent/videodisallowrange"
)

// VideoDisallowRange is the model entity for the VideoDisallowRange schema.
type VideoDisallowRange struct {
	config `json:"-"`
	// ID of the ent.
	ID string `json:"id,omitempty"`
	// StartSeconds holds the value of the "start_seconds" field.
	StartSeconds int `json:"start_seconds,omitempty"`
	// EndSeconds holds the value of the "end_seconds" field.
	EndSeconds int `json:"end_seconds,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the VideoDisallowRangeQuery when eager-loading is set.
	Edges        VideoDisallowRangeEdges `json:"edges"`
	video_id     *string
	selectValues sql.SelectValues
}

// VideoDisallowRangeEdges holds the relations/edges for other nodes in the graph.
type VideoDisallowRangeEdges struct {
	// Video holds the value of the video edge.
	Video *Video `json:"video,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// VideoOrErr returns the Video value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e VideoDisallowRangeEdges) VideoOrErr() (*Video, error) {
	if e.loadedTypes[0] {
		if e.Video == nil {
			// Edge was loaded but was not found.
			return nil, &NotFoundError{label: video.Label}
		}
		return e.Video, nil
	}
	return nil, &NotLoadedError{edge: "video"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*VideoDisallowRange) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case videodisallowrange.FieldStartSeconds, videodisallowrange.FieldEndSeconds:
			values[i] = new(sql.NullInt64)
		case videodisallowrange.FieldID:
			values[i] = new(sql.NullString)
		case videodisallowrange.ForeignKeys[0]: // video_id
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the VideoDisallowRange fields.
func (vdr *VideoDisallowRange) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case videodisallowrange.FieldID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				vdr.ID = value.String
			}
		case videodisallowrange.FieldStartSeconds:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field start_seconds", values[i])
			} else if value.Valid {
				vdr.StartSeconds = int(value.Int64)
			}
		case videodisallowrange.FieldEndSeconds:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field end_seconds", values[i])
			} else if value.Valid {
				vdr.EndSeconds = int(value.Int64)
			}
		case videodisallowrange.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field video_id", values[i])
			} else if value.Valid {
				vdr.video_id = new(string)
				*vdr.video_id = value.String
			}
		default:
			vdr.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the VideoDisallowRange.
// This includes values selected through modifiers, order, etc.
func (vdr *VideoDisallowRange) Value(name string) (ent.Value, error) {
	return vdr.selectValues.Get(name)
}

// QueryVideo queries the "video" edge of the VideoDisallowRange entity.
func (vdr *VideoDisallowRange) QueryVideo() *VideoQuery {
	return NewVideoDisallowRangeClient(vdr.config).QueryVideo(vdr)
}

// Update returns a builder for updating this VideoDisallowRange.
// Note that you need to call VideoDisallowRange.Unwrap() before calling this method if this VideoDisallowRange
// was returned from a transaction, and the transaction was committed or rolled back.
func (vdr *VideoDisallowRange) Update() *VideoDisallowRangeUpdateOne {
	return NewVideoDisallowRangeClient(vdr.config).UpdateOne(vdr)
}

// Unwrap unwraps the VideoDisallowRange entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (vdr *VideoDisallowRange) Unwrap() *VideoDisallowRange {
	_tx, ok := vdr.config.driver.(*txDriver)
	if !ok {
		panic("ent: VideoDisallowRange is not a transactional entity")
	}
	vdr.config.driver = _tx.drv
	return vdr
}

// String implements the fmt.Stringer.
func (vdr *VideoDisallowRange) String() string {
	var builder strings.Builder
	builder.WriteString("VideoDisallowRange(")
	builder.WriteString(fmt.Sprintf("id=%v, ", vdr.ID))
	builder.WriteString("start_seconds=")
	builder.WriteString(fmt.Sprintf("%v", vdr.StartSeconds))
	builder.WriteString(", ")
	builder.WriteString("end_seconds=")
	builder.WriteString(fmt.Sprintf("%v", vdr.EndSeconds))
	builder.WriteByte(')')
	return builder.String()
}

// VideoDisallowRanges is a parsable slice of VideoDisallowRange.
type VideoDisallowRanges []*VideoDisallowRange
