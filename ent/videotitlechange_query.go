// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/KasumiMercury/uo-patradb-dogtrot/ent/predicate"
	"github.com/KasumiMercury/uo-patradb-dogtrot/ent/video"
	"github.com/KasumiMercury/uo-patradb-dogtrot/ent/videotitlechange"
)

// VideoTitleChangeQuery is the builder for querying VideoTitleChange entities.
type VideoTitleChangeQuery struct {
	config
	ctx        *QueryContext
	order      []videotitlechange.OrderOption
	inters     []Interceptor
	predicates []predicate.VideoTitleChange
	withVideo  *VideoQuery
	withFKs    bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the VideoTitleChangeQuery builder.
func (vtcq *VideoTitleChangeQuery) Where(ps ...predicate.VideoTitleChange) *VideoTitleChangeQuery {
	vtcq.predicates = append(vtcq.predicates, ps...)
	return vtcq
}

// Limit the number of records to be returned by this query.
func (vtcq *VideoTitleChangeQuery) Limit(limit int) *VideoTitleChangeQuery {
	vtcq.ctx.Limit = &limit
	return vtcq
}

// Offset to start from.
func (vtcq *VideoTitleChangeQuery) Offset(offset int) *VideoTitleChangeQuery {
	vtcq.ctx.Offset = &offset
	return vtcq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (vtcq *VideoTitleChangeQuery) Unique(unique bool) *VideoTitleChangeQuery {
	vtcq.ctx.Unique = &unique
	return vtcq
}

// Order specifies how the records should be ordered.
func (vtcq *VideoTitleChangeQuery) Order(o ...videotitlechange.OrderOption) *VideoTitleChangeQuery {
	vtcq.order = append(vtcq.order, o...)
	return vtcq
}

// QueryVideo chains the current query on the "video" edge.
func (vtcq *VideoTitleChangeQuery) QueryVideo() *VideoQuery {
	query := (&VideoClient{config: vtcq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := vtcq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := vtcq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(videotitlechange.Table, videotitlechange.FieldID, selector),
			sqlgraph.To(video.Table, video.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, videotitlechange.VideoTable, videotitlechange.VideoColumn),
		)
		fromU = sqlgraph.SetNeighbors(vtcq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first VideoTitleChange entity from the query.
// Returns a *NotFoundError when no VideoTitleChange was found.
func (vtcq *VideoTitleChangeQuery) First(ctx context.Context) (*VideoTitleChange, error) {
	nodes, err := vtcq.Limit(1).All(setContextOp(ctx, vtcq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{videotitlechange.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (vtcq *VideoTitleChangeQuery) FirstX(ctx context.Context) *VideoTitleChange {
	node, err := vtcq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first VideoTitleChange ID from the query.
// Returns a *NotFoundError when no VideoTitleChange ID was found.
func (vtcq *VideoTitleChangeQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = vtcq.Limit(1).IDs(setContextOp(ctx, vtcq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{videotitlechange.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (vtcq *VideoTitleChangeQuery) FirstIDX(ctx context.Context) string {
	id, err := vtcq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single VideoTitleChange entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one VideoTitleChange entity is found.
// Returns a *NotFoundError when no VideoTitleChange entities are found.
func (vtcq *VideoTitleChangeQuery) Only(ctx context.Context) (*VideoTitleChange, error) {
	nodes, err := vtcq.Limit(2).All(setContextOp(ctx, vtcq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{videotitlechange.Label}
	default:
		return nil, &NotSingularError{videotitlechange.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (vtcq *VideoTitleChangeQuery) OnlyX(ctx context.Context) *VideoTitleChange {
	node, err := vtcq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only VideoTitleChange ID in the query.
// Returns a *NotSingularError when more than one VideoTitleChange ID is found.
// Returns a *NotFoundError when no entities are found.
func (vtcq *VideoTitleChangeQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = vtcq.Limit(2).IDs(setContextOp(ctx, vtcq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{videotitlechange.Label}
	default:
		err = &NotSingularError{videotitlechange.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (vtcq *VideoTitleChangeQuery) OnlyIDX(ctx context.Context) string {
	id, err := vtcq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of VideoTitleChanges.
func (vtcq *VideoTitleChangeQuery) All(ctx context.Context) ([]*VideoTitleChange, error) {
	ctx = setContextOp(ctx, vtcq.ctx, "All")
	if err := vtcq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*VideoTitleChange, *VideoTitleChangeQuery]()
	return withInterceptors[[]*VideoTitleChange](ctx, vtcq, qr, vtcq.inters)
}

// AllX is like All, but panics if an error occurs.
func (vtcq *VideoTitleChangeQuery) AllX(ctx context.Context) []*VideoTitleChange {
	nodes, err := vtcq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of VideoTitleChange IDs.
func (vtcq *VideoTitleChangeQuery) IDs(ctx context.Context) (ids []string, err error) {
	if vtcq.ctx.Unique == nil && vtcq.path != nil {
		vtcq.Unique(true)
	}
	ctx = setContextOp(ctx, vtcq.ctx, "IDs")
	if err = vtcq.Select(videotitlechange.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (vtcq *VideoTitleChangeQuery) IDsX(ctx context.Context) []string {
	ids, err := vtcq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (vtcq *VideoTitleChangeQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, vtcq.ctx, "Count")
	if err := vtcq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, vtcq, querierCount[*VideoTitleChangeQuery](), vtcq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (vtcq *VideoTitleChangeQuery) CountX(ctx context.Context) int {
	count, err := vtcq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (vtcq *VideoTitleChangeQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, vtcq.ctx, "Exist")
	switch _, err := vtcq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (vtcq *VideoTitleChangeQuery) ExistX(ctx context.Context) bool {
	exist, err := vtcq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the VideoTitleChangeQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (vtcq *VideoTitleChangeQuery) Clone() *VideoTitleChangeQuery {
	if vtcq == nil {
		return nil
	}
	return &VideoTitleChangeQuery{
		config:     vtcq.config,
		ctx:        vtcq.ctx.Clone(),
		order:      append([]videotitlechange.OrderOption{}, vtcq.order...),
		inters:     append([]Interceptor{}, vtcq.inters...),
		predicates: append([]predicate.VideoTitleChange{}, vtcq.predicates...),
		withVideo:  vtcq.withVideo.Clone(),
		// clone intermediate query.
		sql:  vtcq.sql.Clone(),
		path: vtcq.path,
	}
}

// WithVideo tells the query-builder to eager-load the nodes that are connected to
// the "video" edge. The optional arguments are used to configure the query builder of the edge.
func (vtcq *VideoTitleChangeQuery) WithVideo(opts ...func(*VideoQuery)) *VideoTitleChangeQuery {
	query := (&VideoClient{config: vtcq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	vtcq.withVideo = query
	return vtcq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Title string `json:"title,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.VideoTitleChange.Query().
//		GroupBy(videotitlechange.FieldTitle).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (vtcq *VideoTitleChangeQuery) GroupBy(field string, fields ...string) *VideoTitleChangeGroupBy {
	vtcq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &VideoTitleChangeGroupBy{build: vtcq}
	grbuild.flds = &vtcq.ctx.Fields
	grbuild.label = videotitlechange.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Title string `json:"title,omitempty"`
//	}
//
//	client.VideoTitleChange.Query().
//		Select(videotitlechange.FieldTitle).
//		Scan(ctx, &v)
func (vtcq *VideoTitleChangeQuery) Select(fields ...string) *VideoTitleChangeSelect {
	vtcq.ctx.Fields = append(vtcq.ctx.Fields, fields...)
	sbuild := &VideoTitleChangeSelect{VideoTitleChangeQuery: vtcq}
	sbuild.label = videotitlechange.Label
	sbuild.flds, sbuild.scan = &vtcq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a VideoTitleChangeSelect configured with the given aggregations.
func (vtcq *VideoTitleChangeQuery) Aggregate(fns ...AggregateFunc) *VideoTitleChangeSelect {
	return vtcq.Select().Aggregate(fns...)
}

func (vtcq *VideoTitleChangeQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range vtcq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, vtcq); err != nil {
				return err
			}
		}
	}
	for _, f := range vtcq.ctx.Fields {
		if !videotitlechange.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if vtcq.path != nil {
		prev, err := vtcq.path(ctx)
		if err != nil {
			return err
		}
		vtcq.sql = prev
	}
	return nil
}

func (vtcq *VideoTitleChangeQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*VideoTitleChange, error) {
	var (
		nodes       = []*VideoTitleChange{}
		withFKs     = vtcq.withFKs
		_spec       = vtcq.querySpec()
		loadedTypes = [1]bool{
			vtcq.withVideo != nil,
		}
	)
	if vtcq.withVideo != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, videotitlechange.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*VideoTitleChange).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &VideoTitleChange{config: vtcq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, vtcq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := vtcq.withVideo; query != nil {
		if err := vtcq.loadVideo(ctx, query, nodes, nil,
			func(n *VideoTitleChange, e *Video) { n.Edges.Video = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (vtcq *VideoTitleChangeQuery) loadVideo(ctx context.Context, query *VideoQuery, nodes []*VideoTitleChange, init func(*VideoTitleChange), assign func(*VideoTitleChange, *Video)) error {
	ids := make([]string, 0, len(nodes))
	nodeids := make(map[string][]*VideoTitleChange)
	for i := range nodes {
		if nodes[i].video_id == nil {
			continue
		}
		fk := *nodes[i].video_id
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(video.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "video_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (vtcq *VideoTitleChangeQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := vtcq.querySpec()
	_spec.Node.Columns = vtcq.ctx.Fields
	if len(vtcq.ctx.Fields) > 0 {
		_spec.Unique = vtcq.ctx.Unique != nil && *vtcq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, vtcq.driver, _spec)
}

func (vtcq *VideoTitleChangeQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(videotitlechange.Table, videotitlechange.Columns, sqlgraph.NewFieldSpec(videotitlechange.FieldID, field.TypeString))
	_spec.From = vtcq.sql
	if unique := vtcq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if vtcq.path != nil {
		_spec.Unique = true
	}
	if fields := vtcq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, videotitlechange.FieldID)
		for i := range fields {
			if fields[i] != videotitlechange.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := vtcq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := vtcq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := vtcq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := vtcq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (vtcq *VideoTitleChangeQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(vtcq.driver.Dialect())
	t1 := builder.Table(videotitlechange.Table)
	columns := vtcq.ctx.Fields
	if len(columns) == 0 {
		columns = videotitlechange.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if vtcq.sql != nil {
		selector = vtcq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if vtcq.ctx.Unique != nil && *vtcq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range vtcq.predicates {
		p(selector)
	}
	for _, p := range vtcq.order {
		p(selector)
	}
	if offset := vtcq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := vtcq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// VideoTitleChangeGroupBy is the group-by builder for VideoTitleChange entities.
type VideoTitleChangeGroupBy struct {
	selector
	build *VideoTitleChangeQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (vtcgb *VideoTitleChangeGroupBy) Aggregate(fns ...AggregateFunc) *VideoTitleChangeGroupBy {
	vtcgb.fns = append(vtcgb.fns, fns...)
	return vtcgb
}

// Scan applies the selector query and scans the result into the given value.
func (vtcgb *VideoTitleChangeGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, vtcgb.build.ctx, "GroupBy")
	if err := vtcgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*VideoTitleChangeQuery, *VideoTitleChangeGroupBy](ctx, vtcgb.build, vtcgb, vtcgb.build.inters, v)
}

func (vtcgb *VideoTitleChangeGroupBy) sqlScan(ctx context.Context, root *VideoTitleChangeQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(vtcgb.fns))
	for _, fn := range vtcgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*vtcgb.flds)+len(vtcgb.fns))
		for _, f := range *vtcgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*vtcgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := vtcgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// VideoTitleChangeSelect is the builder for selecting fields of VideoTitleChange entities.
type VideoTitleChangeSelect struct {
	*VideoTitleChangeQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (vtcs *VideoTitleChangeSelect) Aggregate(fns ...AggregateFunc) *VideoTitleChangeSelect {
	vtcs.fns = append(vtcs.fns, fns...)
	return vtcs
}

// Scan applies the selector query and scans the result into the given value.
func (vtcs *VideoTitleChangeSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, vtcs.ctx, "Select")
	if err := vtcs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*VideoTitleChangeQuery, *VideoTitleChangeSelect](ctx, vtcs.VideoTitleChangeQuery, vtcs, vtcs.inters, v)
}

func (vtcs *VideoTitleChangeSelect) sqlScan(ctx context.Context, root *VideoTitleChangeQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(vtcs.fns))
	for _, fn := range vtcs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*vtcs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := vtcs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
